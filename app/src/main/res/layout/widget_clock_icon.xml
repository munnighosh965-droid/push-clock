<?xml version="1.0" encoding="utf-8"?>
<!-- Icon-sized widget: the dial alone, no card, so it sits in the app grid
     and reads as a Power Clock icon that actually keeps time. -->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/icon_widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <AnalogClock
        android:id="@+id/icon_widget_clock"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_gravity="center"
        android:contentDescription="@string/widget_clock_description"
        android:dial="@drawable/widget_clock_dial"
        android:hand_hour="@drawable/widget_hand_hour"
        android:hand_minute="@drawable/widget_hand_minute" />
</FrameLayout>
